<%= form_for(@timedown) do |f| %>
  <% if @timedown.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timedown.errors.count, "error") %> prohibited this timedown from being saved:</h2>

      <ul>
      <% @timedown.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.collection_select :shift_id, Shift.all, :id, :start %>
  </div>
  <div class="field">
    <%= f.label :start %><br />
    <%= f.datetime_select :start %>
  </div>
  <div class="field">
    <%= f.label :end %><br />
    <%= f.datetime_select :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
